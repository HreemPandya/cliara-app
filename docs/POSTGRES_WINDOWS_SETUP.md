# PostgreSQL Setup Guide for Windows

## Status

No manual PostgreSQL setup is required on Windows. Cliara automatically downloads and configures PostgreSQL on your machine when needed.

## Optional: Use Your Own PostgreSQL

If you prefer a custom PostgreSQL instance (local, Docker, or cloud), set the connection string and credentials in `~/.cliara/config.json`. Otherwise, you can ignore PostgreSQL setup entirely.
